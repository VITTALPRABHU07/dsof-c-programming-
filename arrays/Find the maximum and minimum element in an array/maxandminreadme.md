This program finds the maximum and minimum elements in a given array of integers. It reads the number of elements from the user, accepts the array values, and then determines the largest and smallest numbers.

This is a basic and important program in C programming, commonly asked in exams and interviews.
